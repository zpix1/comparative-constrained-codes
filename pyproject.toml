[project]
name = "framework"
version = "0.1.0"
description = "Experimental comparison of constrained coding methods for RLL(0,2)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "pandas>=3.0.3",
]
