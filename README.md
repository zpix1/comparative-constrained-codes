# Constrained Coding Comparison

## Run

Use `uv` (https://docs.astral.sh/uv/):

```bash
uv run python main.py
```

The experiment writes `results/comparison.csv`, `results/comparison.tex`, and
`results/operation_breakdown.tex`.